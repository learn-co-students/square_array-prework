def square_array(array)
    squared_array = []
    array.each {|i| squared_array.push(i**2)}
    return squared_array
end

# in this lab, I was asked to take the numbers from an array and square the values
# and output them into a new array using the .each method.

